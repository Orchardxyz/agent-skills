# One-Page Visual Builder

Build single-screen landing/poster pages with HTML + CSS (Tailwind CDN allowed), producing separate light/dark variants with clean handoff files.

## Installation

```bash
npx skills add https://github.com/Orchardxyz/agent-skills --skill one-page-visual
```
