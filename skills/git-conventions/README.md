# Git Conventions

Generate branch names, commit messages, and PR descriptions that match a repository's actual conventions by inspecting the staging area and commit history.

## Installation

```bash
npx skills add https://github.com/Orchardxyz/agent-skills --skill git-conventions
```
