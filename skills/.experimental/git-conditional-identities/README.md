# Git Conditional Identities

Automatically switch Git `user.name` and `user.email` based on repository location using `includeIf.gitdir` while keeping sensible global defaults.

## Installation

```bash
npx skills add https://github.com/Orchardxyz/agent-skills --skill git-conditional-identities
```
